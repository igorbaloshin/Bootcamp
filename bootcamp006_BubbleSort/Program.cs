using static Sorting;
using static Infrastructure;
Console.Clear();
// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

10.CreateArray(min:1, max:10)
  .Show()
  .SortBubbleOptimized()
  .Show();
